import 'package:flutter/material.dart';

class AuthSettingsView extends StatelessWidget {
  const AuthSettingsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Auth Settings'),
      ),
    );
  }
}
